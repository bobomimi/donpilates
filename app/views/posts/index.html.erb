<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
<div class=container>
<%= link_to t('navbar.title'), root_path, class: "navbar-brand" %>
<button class="navbar-toggler navbar-toggler-right" type=button data-toggle=collapse data-target=#navbarResponsive aria-controls=navbarResponsive aria-expanded=false aria-label="Toggle navigation">
<span class=navbar-toggler-icon></span>
</button>
<div class="collapse navbar-collapse" id=navbarResponsive>
<ul class="navbar-nav ml-auto">
<li class=nav-item>
</li>
<li class=nav-item>
<%= link_to t('navbar.blog'), posts_path, class: "nav-link" %>
</li>
<li class=nav-item>
<%= link_to t('navbar.products'), products_path, class: "nav-link" %>
</li>
<li class=nav-item>
<%= link_to t('navbar.about'), welcome_about_path, class: "nav-link" %>
<li>
<li class=nav-item>
<%= link_to t('navbar.contact'), new_contact_path, class: "nav-link" %>
</li>
<li class=nav-item>
</li>
<li class=nav-item>
</li>
<li class=nav-item>
<% if admin_signed_in? %>
<%= link_to "Sign Out", destroy_admin_session_path, method: :delete, class: "nav-link" %>
<% end %>
</li>
</ul>
</div>
</div>
</nav>
<!-- Page Content -->
    <div class="container">

      <!-- Page Heading/Breadcrumbs -->
      <h1 class="mt-4 mb-3">DonPilates Blog
        <small>Everything you need to know about Pilates</small>
      </h1>

      <ol class="breadcrumb">
        <li class="breadcrumb-item">
          <a href="index.html">Home</a>
        </li>
        <li class="breadcrumb-item active">Blog Home 2</li>
      </ol>
       
      <!-- Blog Post -->
      <% @posts.each do |post| %>
      <div class="card mb-4">
        <div class="card-body">
          <div class="row">
            <div class="col-lg-6">
              <% link_to post do %>
              <%= image_tag post.image, class: "card-img-top"%>
               <% end %>
            </div>
            <div class="col-lg-6">
              <h2 class="card-title"><%= post.main_title %></h2>
              <p class="card-text"><%= post.sub_title %></p>
              <%= link_to 'Read More', post, class: 'btn btn-primary' %>
              <% if admin_signed_in? %>
                <%= link_to 'Edit', edit_post_path(post), class: "btn btn-primary" %>
                <%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-primary" %>
                <%= link_to 'New Post', new_post_path, class: "btn btn-primary" %>
              <% end %>
              </div>
          </div>
        </div>
        <div class="card-footer text-muted">
          Posted on <%= post.created_at.strftime("%B %d %Y, %l:%M%P") %>
        </div>
      </div>
      <% end%>
  <!-- Footer -->
  <footer class="py-5 bg-dark">
    <div class="container">
      <p class="m-0 text-center text-white">Copyright &copy; Your Website 2018</p>
    </div>
    <!-- /.container -->
</footer>
